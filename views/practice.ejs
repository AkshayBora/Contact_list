<html>
    <head>
        <title>
            <%= title %>
        </title>
    </head>
    <body>
        <h1>Playground</h1>

        <ul>
            <% for(let i=1 ; i<10 ; i++) { %>
            <li>
                <%= i %>
            </li>
            <%}%>
        </ul>

        <% let i =1; %>
        <% while(i <=3) {%>
            <h1>hey! i am here</h1>   
            <% i+=1 %> 
        <%} %>

        <!-- <% if(false) { %>
            <h1>It's true!</h1>
        <% } else { %>
            <p>It's false :(</p>
        <% } %> -->
    </body>
</html>